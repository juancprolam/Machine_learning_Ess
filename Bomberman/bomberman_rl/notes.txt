Terminal commands:

## update your branch on github:

git add . 
git commit -m "Message"
git push origin {branch}

## train the agent:
source venv/bin/activate
python main.py play --train 1 --my-agent {agent_name: e.g. brute_force_agent} --n-rounds {n_rounds} --no-gui